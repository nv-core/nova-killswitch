# nova.manifest — app metadata for the nova updater
NAME=nova-killswitch
DESCRIPTION=Fail-closed VPN kill switch: chaining, trusted gateways (IP+MAC), bypass subnets — GNOME extension + GTK app + root nftables backend
VERSION=0.2.0
TYPE=cli+gui
SCOPE=both
INSTALLER=install.sh
